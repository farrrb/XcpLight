# update notifications
notifications:
  email:
    on_success: never

# Enable C support
language: c

# Compiler selection
compiler:
  - gcc

# Build steps
script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
  - ctest --output-on-failure
